<%= form_for(@trading_strategy_set) do |f| %>
  <% if @trading_strategy_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trading_strategy_set.errors.count, "error") %> prohibited this trading_strategy_set from being saved:</h2>

      <ul>
      <% @trading_strategy_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
