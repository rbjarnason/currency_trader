<%= form_for(@trading_strategy_population) do |f| %>
  <% if @trading_strategy_population.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trading_strategy_population.errors.count, "error") %> prohibited this trading_strategy_population from being saved:</h2>

      <ul>
      <% @trading_strategy_population.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
