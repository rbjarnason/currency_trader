<%= form_for(@trading_strategy) do |f| %>
  <% if @trading_strategy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trading_strategy.errors.count, "error") %> prohibited this trading_strategy from being saved:</h2>

      <ul>
      <% @trading_strategy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
