<%= form_for(@trading_time_frame) do |f| %>
  <% if @trading_time_frame.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trading_time_frame.errors.count, "error") %> prohibited this trading_time_frame from being saved:</h2>

      <ul>
      <% @trading_time_frame.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
