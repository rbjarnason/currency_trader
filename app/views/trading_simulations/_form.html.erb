<%= form_for(@trading_simulation) do |f| %>
  <% if @trading_simulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trading_simulation.errors.count, "error") %> prohibited this trading_simulation from being saved:</h2>

      <ul>
      <% @trading_simulation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
