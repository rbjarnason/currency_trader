<%= form_for(@trading_account) do |f| %>
  <% if @trading_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trading_account.errors.count, "error") %> prohibited this trading_account from being saved:</h2>

      <ul>
      <% @trading_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
